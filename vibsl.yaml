services:
  frontend:
    path: ./frontend
    runtime: node
    build: pnpm install && pnpm run build
    start: pnpm preview
    port: 4173

  backend:
    path: ./backend
    runtime: java
    build: ./mvnw package -DskipTests
    start: java -jar target/*.jar
    port: 8080